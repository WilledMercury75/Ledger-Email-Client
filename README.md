# Ledger-Email-Client
A custom email client that can function like regular email clients, with added privacy and customization features. This uses the Peer2Peer system (P2P) since I don't have funds for servers, yet it can send emails to clients outside of the app. 
